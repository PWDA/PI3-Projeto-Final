package br.com.senac.pi3.pwda.validador;

import br.com.senac.pi3.pwda.model.ItemVenda;
import br.com.senac.pi3.pwda.model.Venda;

public class ValidarVenda {
    public static String validar(Venda venda){
//        if (venda == null) {
//            return "Erro na Venda";
//        }
//        if (venda.getCliente() == null || "".equals(venda.getCliente())) {
//            return "É necessário informar um cliente para efetuar a venda";
//        }
//
//        if (venda.getItens() == null
//                || "".equals(venda.getItens())) {
//            return "É necessário selecionar um produto para efetuar a venda";
//        }
        return null;
    }    
    public static String validarItemVenda(ItemVenda itemVenda){
//        if (itemVenda == null) {
//            return "Erro na Venda";
//        }
//        if (itemVenda.getProduto()== null || "".equals(itemVenda.getProduto())) {
//            return "Não existem Produto selecionado";
//        }
//
//        if (itemVenda.getQuantidade() <= 0 || "".equals(itemVenda.getQuantidade())) {
//            return "É necessário informar uma quantidade";
//        }
        return null;
    }
}
